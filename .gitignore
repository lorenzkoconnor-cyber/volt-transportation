# macOS
.DS_Store
.AppleDouble
.LSOverride

# Claude Code internal
.claude/

# Secrets — never commit
volt-app/.env.local
volt-app/.env

# Large media originals (source files — video is already in public/)
*.mov
*.mp4
!volt-app/public/images/*.mp4
